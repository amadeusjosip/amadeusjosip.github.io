const observer1 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-1');
        }
        else{
            entry.target.classList.remove('scroll-1');
        }
 
    });
}); 

const hiddenElements1 = document.querySelectorAll('.item-1');
hiddenElements1.forEach((el) => observer1.observe(el));


////////////////////////////////////////////////////////////

const observer2 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-2');
        }
        else{
            entry.target.classList.remove('scroll-2');
        }
 
    });
}); 

const hiddenElements2 = document.querySelectorAll('.item-2');
hiddenElements2.forEach((el) => observer2.observe(el));

//////////////////////////////////////////////////////////////////

const observer3 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-3');
        }
        else{
            entry.target.classList.remove('scroll-3');
        }
 
    });
}); 

const hiddenElements3 = document.querySelectorAll('.item-3');
hiddenElements3.forEach((el) => observer3.observe(el));

//////////////////////////////////////////////////////////////////

const observer4 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-4');
        }
        else{
            entry.target.classList.remove('scroll-4');
        }
 
    });
}); 

const hiddenElements4 = document.querySelectorAll('.item-4');
hiddenElements4.forEach((el) => observer4.observe(el));

//////////////////////////////////////////////////////////////////

const observer5 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-5');
        }
        else{
            entry.target.classList.remove('scroll-5');
        }
 
    });
}); 

const hiddenElements5 = document.querySelectorAll('.item-5');
hiddenElements5.forEach((el) => observer5.observe(el));

//////////////////////////////////////////////////////////////////

const observer6 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-6');
        }
        else{
            entry.target.classList.remove('scroll-6');
        }
 
    });
}); 

const hiddenElements6 = document.querySelectorAll('.item-6');
hiddenElements6.forEach((el) => observer6.observe(el));

//////////////////////////////////////////////////////////////////

const observer7 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-7');
        }
        else{
            entry.target.classList.remove('scroll-7');
        }
 
    });
}); 

const hiddenElements7 = document.querySelectorAll('.item-7');
hiddenElements7.forEach((el) => observer7.observe(el));

//////////////////////////////////////////////////////////////////

const observer8 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-8');
        }
        else{
            entry.target.classList.remove('scroll-8');
        }
 
    });
}); 

const hiddenElements8 = document.querySelectorAll('.item-8');
hiddenElements8.forEach((el) => observer8.observe(el));

//////////////////////////////////////////////////////////////////

const observer9 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-9');
        }
        else{
            entry.target.classList.remove('scroll-9');
        }
 
    });
}); 

const hiddenElements9 = document.querySelectorAll('.item-9');
hiddenElements9.forEach((el) => observer9.observe(el));

//////////////////////////////////////////////////////////////////

const observer10 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-10');
        }
        else{
            entry.target.classList.remove('scroll-10');
        }
 
    });
}); 

const hiddenElements10 = document.querySelectorAll('.item-10');
hiddenElements10.forEach((el) => observer10.observe(el));

//////////////////////////////////////////////////////////////////

const observer11 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-11');
        }
        else{
            entry.target.classList.remove('scroll-11');
        }
 
    });
}); 

const hiddenElements11 = document.querySelectorAll('.item-11');
hiddenElements11.forEach((el) => observer11.observe(el));
//////////////////////////////////////////////////////////////////

const observer12 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-12');
        }
        else{
            entry.target.classList.remove('scroll-12');
        }
 
    });
}); 

const hiddenElements12 = document.querySelectorAll('.item-12');
hiddenElements12.forEach((el) => observer12.observe(el));
//////////////////////////////////////////////////////////////////

const observer13 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-13');
        }
        else{
            entry.target.classList.remove('scroll-13');
        }
 
    });
}); 

const hiddenElements13 = document.querySelectorAll('.item-13');
hiddenElements13.forEach((el) => observer13.observe(el));


////////////////////slika------------------------



//////////////////////////////////////////////////////////////////

const observerA = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-A');
        }
        else{
            entry.target.classList.remove('scroll-A');
        }
 
    });
}); 

const hiddenElementsA = document.querySelectorAll('.moje-ime');
hiddenElementsA.forEach((el) => observerA.observe(el));


//////////////////////////////////////////////////////////////////

const observerB = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-B');
        }
        else{
            entry.target.classList.remove('scroll-B');
        }
 
    });
}); 

const hiddenElementsB = document.querySelectorAll('.slika');
hiddenElementsB.forEach((el) => observerB.observe(el));
//////////////////////////////////////////////////////////////////

const observerC = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-C');
        }
        else{
            entry.target.classList.remove('scroll-C');
        }
 
    });
}); 

const hiddenElementsC = document.querySelectorAll('.p-opis');
hiddenElementsC.forEach((el) => observerC.observe(el));

//////////////////////////////////////////////////////////////////

const observerC2 = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
        console.log(entry) 
        if(entry.isIntersecting){
            entry.target.classList.add('scroll-C2');
        }
        else{
            entry.target.classList.remove('scroll-C2');
        }
 
    });
}); 

const hiddenElementsC2 = document.querySelectorAll('.p-opis2');
hiddenElementsC2.forEach((el) => observerC2.observe(el));